import { NavLink } from 'react-router-dom'
import { BarChart3, BookOpen, BrainCircuit, FileText, History, LayoutDashboard, LogOut, MessageSquare, Search, Settings, UserRound, X } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
const links = [['/app','Dashboard',LayoutDashboard],['/app/chat','AI Assistant',MessageSquare],['/app/courses','My Courses',BookOpen],['/app/materials','Study Materials',FileText],['/app/quiz','Quiz Generator',BrainCircuit],['/app/search','Search',Search],['/app/history','History',History],['/app/profile','Profile',UserRound]]
export default function Sidebar({ open, onClose }) { const { logout } = useAuth(); return <aside className={`sidebar ${open ? 'open' : ''}`}><div className="side-brand"><span className="logo-mark">✦</span><span>Study<span>AI</span></span><button className="mobile-close" onClick={onClose}><X size={18}/></button></div><p className="nav-label">WORKSPACE</p><nav>{links.map(([to,label,Icon]) => <NavLink key={to} to={to} end={to === '/app'} onClick={onClose}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="side-bottom"><NavLink to="/app/settings" onClick={onClose}><Settings size={18}/><span>Settings</span></NavLink><button className="logout" onClick={logout}><LogOut size={18}/><span>Log out</span></button></div></aside> }
