import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { ArrowRight } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
export default function Register(){const {register,loading}=useAuth();const navigate=useNavigate();const [form,setForm]=useState({full_name:'',email:'',password:'',confirm_password:'',college:'',department:''});const [error,setError]=useState('');const change=e=>setForm({...form,[e.target.name]:e.target.value});const submit=async e=>{e.preventDefault();setError('');try{await register(form);navigate('/app')}catch(err){setError(err.response?.data?.detail||'Unable to create your account.')}};return <div className="auth-page"><div className="auth-aside register-aside"><Link to="/" className="side-brand"><span className="logo-mark">✦</span><span>Study<span>AI</span></span></Link><div><p className="kicker">A smarter starting point</p><h1>Your courses, finally connected.</h1><p>Bring your material together and make every study session count.</p></div></div><main className="auth-main"><div className="auth-form register-form"><p className="kicker">Get started</p><h2>Create your account</h2><p className="form-subtitle">Your academic workspace is a few details away.</p><form onSubmit={submit}><div className="form-row"><label>Full name<input name="full_name" required value={form.full_name} onChange={change} placeholder="Alex Morgan"/></label><label>Email address<input type="email" name="email" required value={form.email} onChange={change} placeholder="you@college.edu"/></label></div><div className="form-row"><label>College / university<input name="college" value={form.college} onChange={change} placeholder="Your institution"/></label><label>Course / department<input name="department" value={form.department} onChange={change} placeholder="Computer Science"/></label></div><div className="form-row"><label>Password<input type="password" name="password" minLength="8" required value={form.password} onChange={change} placeholder="At least 8 characters"/></label><label>Confirm password<input type="password" name="confirm_password" required value={form.confirm_password} onChange={change} placeholder="Repeat password"/></label></div>{error&&<p className="error-text">{error}</p>}<button className="primary-button full" disabled={loading}>{loading?'Creating account':<>Create account <ArrowRight size={16}/></>}</button></form><p className="auth-switch">Already have an account? <Link to="/login">Sign in</Link></p></div></main></div>}
