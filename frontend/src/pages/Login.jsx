import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { ArrowRight, Eye, EyeOff, Sparkles } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
import Toast from '../components/Toast'
export default function Login() { const { login, loading } = useAuth(); const navigate=useNavigate(); const [form,setForm]=useState({email:'',password:''}); const [error,setError]=useState(''); const [show,setShow]=useState(false); const submit=async e=>{e.preventDefault();setError('');try{await login(form);navigate('/app')}catch(err){setError(err.response?.data?.detail || (err.code === 'ERR_NETWORK' ? 'Cannot reach the StudyAI server. Make sure the backend is running on port 8000.' : 'Unable to sign in. Please try again.'))}};return <div className="auth-page"><div className="auth-aside"><Link to="/" className="side-brand"><span className="logo-mark">✦</span><span>Study<span>AI</span></span></Link><div><p className="kicker">Your study, in focus</p><h1>Make the next concept click.</h1><p>One thoughtful workspace for questions, notes, and progress.</p></div><small>© 2026 StudyAI</small></div><main className="auth-main"><div className="auth-form"><div className="mobile-auth-brand"><span className="logo-mark">✦</span> Study<span>AI</span></div><p className="kicker"><Sparkles size={14}/> Welcome back</p><h2>Sign in to StudyAI</h2><p className="form-subtitle">Pick up where your last study session ended.</p><form onSubmit={submit}><label>Email address<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@college.edu"/></label><label>Password<div className="password-field"><input type={show?'text':'password'} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Your password"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><div className="form-meta"><label className="check"><input type="checkbox"/> Remember me</label><a href="#forgot">Forgot password?</a></div><button className="primary-button full" disabled={loading}>{loading?'Signing in...':<>Sign in <ArrowRight size={16}/></>}</button>{error&&<Toast message={error} onClose={()=>setError('')}/>}</form><p className="auth-switch">New to StudyAI? <Link to="/register">Create an account</Link></p></div></main></div> }
